apiVersion: v1
kind: Service
metadata:
  name: {{ include "brunocampos.fullname" . }}
  labels:
    {{- include "brunocampos.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "brunocampos.selectorLabels" . | nindent 4 }}
